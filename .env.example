# 환경 변수 예시 (실제 값으로 교체)
# 이 파일은 절대 실제 비밀번호/토큰 값을 저장하지 않습니다.

# PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/alrimjang
DB_USERNAME=alrimjang_app
DB_PASSWORD=change_me

# App
PORT=9090
SPRING_PROFILES_ACTIVE=dev
